[package]
name = "DotToDotStudio"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.36"
egui = "0.36"
image = "0.25.10"
rfd = "0.17.2"
rusqlite = { version = "0.40", features = ["bundled"] }
base64 = "0.23"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"
